# Moved to workspace tools

Implementation: `~/workspace_tools/backup/backup_system`.
Run: `~/workspace_tools/backup/run.sh`.
This small import bridge keeps old commands/integrations compatible.
The local tools must be restored from backup; cloning SmartSignalHub alone does not supply them.
